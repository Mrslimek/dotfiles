# MPD runtime files (regenerated by mpd on every run).
database
state
sticker.sql
pid
log

# Playlists are user-created but stored here at runtime; keep them
# out of the repo to avoid churning diffs.
playlist/*
