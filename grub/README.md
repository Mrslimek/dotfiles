# GRUB Configuration

## Files
- `etc/default/grub` → main configuration file for GRUB.

## Installation with GNU Stow

Run the following from the root of the dotfiles repo:

```bash
sudo stow -t / grub
